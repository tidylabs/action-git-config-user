{
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dist": "ncc build index.js --license licenses.txt --minify",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --coverage"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@octokit/graphql": "^8.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0"
  },
  "jest": {
    "transform": {}
  }
}
